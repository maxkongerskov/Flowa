# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
*.xcuserdatad/
build/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager — track Package.resolved (pins WhisperKit), ignore checkouts
.swiftpm/
.build/
Packages/

# Speech model ships in the app (~1.5 GB) but is too large for git
# (AudioEncoder weights ~1.2 GB). Keep local for Archive/notarize builds.
Flowa/Models/openai_whisper-large-v3-v20240930_turbo/

# Local export / notarize output
build/

# Backups
*.bak
*.bak.*
.git.backup/
