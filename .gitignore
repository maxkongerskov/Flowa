# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
*.xcuserdatad/
build/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager — track Package.resolved (pins WhisperKit), ignore checkouts
.swiftpm/
.build/
Packages/

# Speech engine (~1.5 GB) is too large for git. Not required to clone/build:
# first launch downloads it from the WhisperKit hub. Keep a local copy here
# only when you want to Archive a fully offline app package.
Flowa/Models/openai_whisper-large-v3-v20240930_turbo/

# Local export / notarize output
build/

# Backups
*.bak
*.bak.*
.git.backup/
